# Cloud Links Hub (Render)

Niche-based content hub deployed on Render.com.

## Endpoints

- `GET /` — Homepage with niche index
- `GET /:niche` — Niche landing page
- `GET /:niche/:slug` — Individual post page
- `POST /api/posts` — Create/update a post
- `GET /api/posts` — List all posts
